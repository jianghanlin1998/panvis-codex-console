# Step 10 — Context, task controls and simpler workflow

Approved by Hanlin on 2026-09-09. This changes Console behavior; it does not approve or restart AI Update Board implementation.

## User workflow

1. Choose a project folder. Console detects the repository, display name and current branch; manual entry remains available. A folder without initial Git history gets an actionable explanation.
2. Discuss the goal in project, task or subtask chat. Suggested children have their own pages and conversations before execution is approved.
3. Confirm product direction. Planning can investigate local files, repository status, saved task state and discussion history through a read-only tool. Technical evidence gathering does not require a separate product approval.
4. Inspect the plan and choose each task's checking depth. New work defaults to planner self-check; independent plan review is optional. The owner confirms the implementation plan once.
5. Execute with the selected checks: implementation/basic verification, independent QA with feedback on the second failed QA attempt, or hardening/QA with feedback on the third. Context and accepted product direction remain available to the assigned role.
6. Pause at the current role's save boundary, continue, or end and retain records. Reopening does not repeat a completed role. Ending work never fabricates QA or product acceptance.
7. Inspect the deliverable: changed files, usage instructions and appropriate output links. Local web launch is offered only for detected web results. Git revision details and long historical closeout notes are expandable.

Projects, big tasks and subtasks have explicit ownership and navigation. Completed big tasks collapse in place. Ended projects collapse and appear below active projects. Candidate subtask review changes follow the new immutable plan version while preserving human context and per-scope settings.

## Context and model boundary

- Chats accept PNG, JPEG and WebP by file upload, paste or drop: at most six images, four MiB each. Images are stored locally with project ownership and sent as actual model image inputs. Text is escaped in the UI.
- Chats can inspect their saved plans, review findings, failure state, history and local repository. Long conversations carry a generated context summary; original messages remain retained.
- Planning includes bounded human feedback from candidate subtasks, draft children and previous plan versions. Execution and QA receive human reference material without implementation-agent conversations or verdicts. New product requests remain reference material until reconciled with the approved task contract.
- Current task images take precedence over older parent images. A maximum of six is sent. Large histories remain accessible through bounded read-only history requests.
- The model has a scoped `console_read` dynamic tool for investigation. The 2026-09-10 follow-up below adds native read-only shell, web search and image viewing; project writes and approval escalation remain unavailable in planning and discussion. Tool output is untrusted reference data; it cannot grant implementation authority.
- Provider output uses the supported strict object schema: all properties are required, including the context summary, while stored historical responses remain readable without the new field.

## Usage and approvals

New work defaults to measurement and reminders. A successful turn with unavailable usage is kept successful and marked unknown. Actual model failures still stop; they are never converted into success or replayed automatically. A selected hard budget still stops when reached or when required usage is unknown. The selected duration remains an execution window.

The policy is carried through planning, aggregate execution, provider claim, persisted success provenance and completion validation. Existing approvals without the new policy retain their historical limits and digests. Changing defaults affects future plans; it does not silently rewrite an approved task.

## QA repairs made during this change

- Reconciled successful unknown usage at all three persistence/validation boundaries, not just the visible budget check.
- Corrected candidate pause handling and cooperative in-flight pause so a paused dependency is not reported as an unexplained governed failure.
- Preserved human context across successive plan revisions, and included child feedback in replanning.
- Corrected image priority and the SELF amendment message that incorrectly promised independent review.
- Fixed strict provider schema compatibility with a real synthetic read-file/image turn.
- Preserved draft ordering, parent/child grouping, lifecycle display and selected task checking depth.
- Removed repeated approval parsing and full execution-state replay from known-usage hot paths; missing-usage exceptions still perform the same authority checks.
- Isolated two older tests' private Git/SQLite preparation using per-test hooks. Changed a repair-loop test's busy state polling to role-completion notifications. Operation ordering, all business assertions and existing timeouts remain unchanged.
- Updated the explicit current migration/table inventories and reconstructed predecessor trigger definitions for schema-upgrade tests. Historical migration counts stay pinned.

## Verification and activation

Focused checks cover these behaviors and existing browser/HTTP execution from approval to owner acceptance. Compiled executable E2E passed daemon startup/race, governed commands, signal handling and cleanup with zero real provider turns or target writes. A real synthetic model smoke read a verification file and identified a red/blue image: 28,487 reported tokens, about 22 seconds, no tool approval. Earlier failed probes reported unavailable usage; that successful count is not an all-attempt total. No Board work was started.

Browser interaction covered candidate pages, actual image upload/persistence, pause/continue, end/reopen, plan-level checking changes, and narrow layout without horizontal overflow. macOS native folder point-and-click could not finish because the host locked; command construction, cancellation, Unicode/spaced paths and repository detection have deterministic coverage. Windows has not been exercised on a Windows runner.

Final unified verification on 2026-09-10: **197 files / 4,891 tests PASS**, four workers, 653.59 seconds, no failures, errors or skips. Public hygiene, lint, typecheck, build, compiled executable E2E and diff checks PASS. Earlier full attempts exposed migration expectations and fixture timing/polling failures; those attempts are not relabeled as passes. Independent QA found no remaining blocker after the context, usage-policy and fixture corrections.

Activated through the unchanged, signature-verified desktop app after a private mode-0600 schema-30 backup. All **54 existing table record sets** have identical before/after hashes; the new context table starts empty. SQLite integrity and foreign-key checks PASS. Actual authenticated HTTP readback confirms six unmaterialized subtask pages with discussion/context, the original four-child CLOSED Board delivery and exact result revision, and no new Board execution. macOS locked during the native folder point-and-click check, so that interaction and Windows execution remain unverified. Schema 31 adds local context/image entries and updates the governed success trigger only for newly approved measurement-mode work. Preserve a private schema-30 backup before activation. Rollback requires retaining the new database separately and restoring the matching old database and binary; do not open schema 31 with an old binary or discard user history.


## 2026-09-10 — Working chat actions and larger context

Approved follow-up: repair failed re-investigation and chat progression, broaden available investigation tools, and replace restrictive text/context capacity limits with 1 MiB. This does not authorize new Board execution or reopen tasks ended by the owner.

The retained intake and six suggested children together exceeded the old 24,000-byte planning acceptance limit. `prepareAgain` now preserves them under the larger limit, reuses an existing successor and rejects incompatible active/ended states. The two reported chat failures stored `TOOL_ACTION_ATTEMPTED`: native command events were rejected even though the model could attempt them. Those stored failures remain unchanged; the UI now explains their category.

Big-task and subtask chats can request scoped advance/pause operations through the same application services as the controls. Requests become durable outcomes with navigation links; failures are displayed as unfinished actions. Implementation still requires the owner's current plan confirmation. An ended task must explicitly be reopened; a subtask cannot advance its parent or sibling. Interrupted pending requests are not silently replayed after restart.

Discussion messages/replies/summaries, durable human notes, planning intake/output, compiled role context and structured role results support 1,048,576 UTF-8 bytes. Envelopes are larger where they carry several blocks, JSON escaping, conversation history or attachments. Compact titles, IDs and task-count constraints remain semantic limits. Ordinary context targets and user-selected hard budgets are unchanged. This increases Console capacity; it does not promise that every model can consume a 1 MiB prompt.

Investigation now supports scoped Console state/history, current and retained-revision repository files, native read-only shell, web search/page open/find and image viewing. Authorized owned-worktree implementation/QA accepts search/image events as well. Configured unrelated MCP servers remain disabled. No new browser automation package, plugin or credential was installed. Availability and event handling were verified with mocked provider streams, not a real network/model run.

Schema 32 rebuilds only governed provider-claim and result-provenance tables to raise two capacity checks, preserving their guards and referencing triggers. A production database copy migrated successfully with all 55 table record sets unchanged and no foreign-key errors; retained replanning with all six children also passed on the copy.

Verification: the full applicable suite ran 198 files / 4,900 tests; 4,892 passed and eight failed on old boundary fixtures or fixture setup timing. After correcting those fixtures without weakening assertions/timeouts, affected grouped verification covered 532 tests (530 passed; two additional old operator-boundary expectations were then corrected). The final release group passed 10 files / 321 tests, including those corrections and a new execution search/image case. No second whole-suite run or real provider turn was used. The compiled executable test likewise required its old 64 KiB response fixture to move to the new 1 MiB threshold. Windows execution and live browser/search availability are not claimed.


Release checks passed: `pnpm public:check`, `pnpm lint`, `pnpm typecheck`, `pnpm build`, `pnpm test:local-control:e2e` and `git diff --check`. Desktop activation completed after an idle check and a private schema-31 backup. All 55 table record sets remain identical, schema 32 integrity/foreign-key checks pass, and the unchanged signature-verified desktop app reopened with the new chat controls. No task lifecycle or execution was changed. For rollback, retain the schema-32 database separately and restore the matching schema-31 backup and prior binary; do not run an old binary against the upgraded database.


## 2026-09-10 — Stable controls, selectable models and visible activity

The four-second busy-state refresh rebuilt the page and reset native disclosure panels. Refresh now retains nested open/closed states by page and preserves unsaved form fields. While context/settings are expanded, the composer stays in normal document flow so it cannot cover those controls. Old transient notices expire; opening a panel does not submit product direction. Chat uses Enter to send and Shift+Enter for a newline; IME composition, repeated keydown and busy sends are guarded.

A dedicated “模型与推理” control appears in chats, with project defaults also accessible from “任务偏好”. Project settings inherit to tasks and subtasks, with individual overrides and reset-to-inherit. Draft confirmation and replanning retain the selected model. These settings apply to the next discussion/planning turn, leaving an active turn and approved implementation model unchanged. Opening settings refreshes the available catalogue through Codex `model/list`; ordinary reads cache it for five minutes. New model IDs and supported reasoning levels are not hardcoded. A catalogue failure preserves existing selection; unavailable saved models are shown explicitly. Source: [official App Server model discovery](https://learn.chatgpt.com/docs/app-server#list-models-modellist).

Actual local catalogue discovery succeeded without creating a provider thread/turn: the current inherited default is `gpt-6-astra` / `high`. The reported planner ran from 11:37:41 to 11:43:53 Beijing time and completed, with 438,431 reported tokens; the screenshot predates completion. That record cannot establish how much time was spent on each tool because earlier planning had no activity telemetry. New discussion and planning runs retain throttled safe activity categories, last activity time and selected/observed model metadata. UI distinguishes recent signals from absent/stale signals and never invents a percent-complete value. No commands, provider errors or internal reasoning text are stored as progress telemetry.

Verification: 27 focused tests passed. One full applicable run covered 200 files / 4,909 tests: 4,907 passed and two older expectations failed (search classified as unknown; old 64 KiB response threshold). Those fixtures now test an actually unknown tool and an envelope larger than 1 MiB. Grouped rechecks passed 5 files / 38 tests, including model selection retained on replanning; the full suite was not repeatedly run. Typecheck, lint, build, public hygiene, compiled executable E2E and diff checks passed. A Chromium browser check against a private database copy passed nested disclosures across two actual polls, no unintended writes, model save/readback, Enter/Shift+Enter and IME protection. The local model catalogue was read; all content-generation verification used mocks and no Board execution was started. Windows was not exercised.

Activation completed through the unchanged signature-verified desktop app after an idle check and private backup. Schema remains 32; all 55 table record sets are unchanged and SQLite integrity/foreign-key checks pass. Rollback must preserve subsequent data and restore the matching pre-activation database and binary because older strict readers do not understand the new optional model/activity fields.

## 2026-09-10 preparation failure and continuation

- The reported PREPARE_ROLE / INVALID_INPUT was reproduced on a private database copy with provider execution and worktree mutations disabled. A valid six-subtask plan's canonical binding exceeded an overlooked 2,048-character workflow transition limit. The matching graph-change parser had the same stale limit. Both now accept up to 1 MiB UTF-8 while retaining exact identity and authority checks. The retained real task now reaches ROLE_AUTHORIZED on the copy without calling a model.
- Local CHECK_LIMITS / PREPARE_ROLE failures use the existing start operation after checking the approved plan, repository, settled usage, safe checkpoint, pending integration, lifecycle and original limits. Button and scoped chat use the same path. Provider-stage failures still require their appropriate recovery; no model, token allowance, repair count or deadline is silently changed. Original failure events remain readable after restart.
- Project review depth now labels its built-in default clearly. The model preferences page explains that the local default is read from Codex config.toml; project/chat overrides remain the editable Console controls. No new global configuration writer was introduced.
- Verification: full suite ran 201 files / 4,917 tests: 4,916 passed, one existing raw-socket upload test hit an early-rejection EPIPE race. Its fixture now verifies the same exact body limit and immediate oversized declared-length rejection without racing a large write; all 53 tests in the four-file affected group pass. The sandbox-only attempt was stopped after loopback-listen denial, then the complete suite ran with local test ports enabled. Public hygiene, lint, typecheck, build and compiled executable E2E pass. Headless Chromium on a private copy of the reported task verified one retry request, no model calls, unchanged limits/history, and the project default label. Activated through the unchanged desktop app after a private backup: schema 32 and all 55 table record sets preserved. No real Board execution or model calls were initiated. Windows runtime verification is unavailable.


## 2026-09-10 execution progress and rejected QA output

- Read-only retained-run investigation distinguishes two failures. Initial Astra execution ended with TURN_INTERRUPTED after 138,595 tokens under a historical 120,000-token subtask bound. The Sol/xhigh recovery changed both the model and subtask budget to warning mode; its execution succeeded with 551,660 tokens. This comparison does not establish a model-capability difference. The subsequent Sol QA ended with STRUCTURED_RESULT_INVALID after 516,573 tokens; aggregate known usage was 1,206,828, below the approved 2,000,000 total. Diagnostics recorded no approval rejection for these runs. The failed QA payload was not retained, so the exact invalid field cannot be reconstructed.
- Governed EXECUTE/REPAIR/checking turns now send a role-specific outputSchema at generation time, matching the existing five-key result contract, outcomes and field bounds. Previously only prompt text described that contract. Storage still checks semantic consistency; no invalid QA result is accepted as a pass, and historical budgets/approvals are not rewritten. The governed mock refuses requests without the matching schema, while malformed-result scenarios still exercise rejection.
- Big-task headers show integrated children, the active child/role and safe latest activity. Subtask headers show implementation/check stages, repairs, completion, waiting or stopped state, with activity only for that child. The existing task API supplies parent execution status, so no API/schema migration is required. Active child pages poll, missing reads retry with an explicit unknown-progress message, stale/offline activity does not pulse, and current model completion percentages are not invented. Recovery text explains result-format rejection and no longer presents the already-applied subtask warning mode as a new adjustment.
- Verification: focused group 60/60 PASS; full suite **201 files / 4,922 tests PASS**, four workers, 554.51 seconds, no failures/errors/skips. Public hygiene, lint, typecheck, build, compiled executable E2E and diff checks PASS. Chromium against a private database copy and synthetic live activity verifies desktop/390-pixel layout, child polling and progress across tabs, with zero real model calls or production task writes. Windows and a new real-provider structured-result turn were not exercised.
- Activation: verified idle, private backup, normal restart through the unchanged signature-verified desktop entry. Schema remains 32; hashes of all 55 table record sets are unchanged, and SQLite integrity/foreign-key checks pass. No Board work was resumed. For code rollback, use the previous matching build only when idle and preserve subsequent records; this change itself requires no database migration or restoration.


## 2026-09-10 pending pause cancellation and elapsed labels

- Read-only live investigation found that the reported B execution started at 19:39 and still emitted THINKING/READING_OR_TESTING activity at 19:46 and 19:51. The older on-screen timestamp alone did not establish a stalled model. The big-task elapsed number was wall time since its first start, including pauses; it was not productive execution time or the current role duration.
- A lifecycle pause deliberately lets the in-flight role reach its save boundary. The UI nevertheless disabled Continue whenever any role remained active, preventing cancellation of that pending pause. It now distinguishes pending from completed pause and enables Cancel pause / continue while the current role runs. The existing lifecycle API clears the pause without starting a duplicate role; ended-task behavior remains unchanged.
- Elapsed labels now distinguish current-round elapsed time from time since first start including pauses. A direct Update progress control reads current state without a model call. The exact cause of the user's stale browser snapshot was not reproduced; no unverified polling fix is claimed.
- Verification: private-copy Chromium interaction PASS for pending-pause cancellation, no duplicate start, updated labels and explicit progress refresh. The existing gated-provider lifecycle regression now also cancels and reapplies pause before allowing the same role to finish, then resumes normally, preserving all original checks. Full suite **201 files / 4,923 tests PASS**, four workers, 638.72 seconds, no failures/errors/skips. Lint, typecheck, build, public hygiene and diff checks PASS. No production lifecycle change or real model retry was initiated; only source-served browser assets change, so no daemon restart or database migration is required.

- Hanlin confirmed that the discussion-page Continue advancing button cleared the pending pause while the disabled header button did not. A read-only HTTP asset check confirms the running daemon serves the revised header and elapsed labels without restart. Windows was not exercised.


## 2026-09-10 result reception and interruption diagnostics

- Read-only investigation found two completed REPAIR turns rejected in the RESULT phase with GOVERNED_AUTHORITY_REQUIRED. Both subtask and total usage were warning-only; aggregate usage did not itself trigger this stop. The failed payloads were not retained, so the exact historical rejected condition remains unknown. The second attempt was already a recovery: the backend rejects another recovery of that authorization, while the previous UI still offered it without explaining the conflict. Five modified Board files remain in their existing worktree.
- A deterministic reproduction established a result-saving defect: a write-enabled role could edit files and legitimately report BLOCKED, but only READY/PASS outcomes received a coordinator checkpoint. The resulting dirty candidate caused rejection and discarded the blocker summary. Write-enabled outcomes now checkpoint preserved edits before saving the result; BLOCKED remains blocked and never becomes accepted or integrated. This reproduced defect is not asserted to be the proven cause of the unavailable historical payloads.
- Result failures now distinguish candidate validation, checkpoint creation, usage/provenance metadata, record saving and state reconciliation with bounded safe codes. Raw exceptions and provider output are not exposed. The readonly task response includes the latest role's canonical summary so a new semantic blocker does not display an older transport failure. There is no database migration; older binaries may not understand newly written failure codes.
- Task pages and interruption dialogs explain why, where, preserved work and next action. A used recovery or expired window is explained immediately, without a doomed API request. Other recovery errors appear inside the dialog; refreshing explicitly says it only reads state. STANDARD independent QA correctly shows implementation as the first completed stage. Recovery limits, approved QA policy and historical records are not reset, and no production model retry is initiated.
- Verification: the new blocked-edit regression failed before the fix and passes afterward. Full suite **202 files / 4,929 tests PASS**, four workers, 566.49 seconds; public hygiene, lint, typecheck, build, compiled executable E2E and diff checks PASS. Private-copy Chromium verified readable interruption details, exhausted-recovery handling, refresh feedback and inline server-failure handling, with zero real model calls or production writes. Windows and a new real-provider result were not exercised.
- Activation: idle check, private backup and restart through the unchanged signature-verified desktop app completed. Schema remains 32; all 55 table record sets are unchanged and integrity/foreign-key checks pass. The existing Board task remains stopped with its worktree preserved. For rollback, stop only while idle and use a binary compatible with any newly saved failure codes; preserve subsequent records rather than restoring an old database over new work.


## 2026-09-10 owner-adjustable interruption recovery

- The previous diagnostic release explained the stop but left an exhausted recovery with no usable continuation path. The owner can now increase the per-step technical recovery count and amend total token control/reference and cumulative model-call limits in the stopped task. Expired time can be renewed in the same dialog. Defaults preserve the current settings except suggesting one additional recovery when exhausted. Saving and continuing is an explicit UI action; historical usage, failures, code and actual QA outcomes are preserved. Technical recovery does not erase QA failures or enlarge QA repair cycles.
- LIMITS_ADJUSTED is an append-only, revision-bound event; stale changes, active execution and pending integration are rejected. Repeating the same amendment is idempotent. Hard budget mode and warning-only mode use the latest explicit choice; limit changes do not reset accumulated usage. Recovery authorizations form a derived chain with increasing ordinals, each failed attempt recovered only once. Existing single-recovery records remain readable. Migration 33 expands the recovery ordinal trigger while preserving uniqueness and immutability; the canonical schema fingerprint is updated.
- Verification: full suite ran **203 files / 4,932 tests**: 4,900 passed; 29 failures were the shared expected migration count still at 32 and three were the time-only renewal guard regression. The count is now 33 and original renewal guards are restored. All affected checks passed in grouped rechecks: **13 files / 275 migration tests**, **2 files / 4 continuation/adjustment tests**, and **1 file / 5 window tests**. Lint, typecheck, build, public hygiene, executable E2E and diff checks pass. A private copy of the actual blocked Board task reached RUNNING through the browser adjustment dialog and real local recovery methods, with unchanged history/usage and one simulated start; no real provider turn or production task write. Desktop and 390-pixel layouts inspected.

- Activated 2026-09-11 through the unchanged signature-verified desktop entry after an idle check and private backup. Schema 32 → 33; all 55 table record sets unchanged, integrity and foreign-key checks pass. No real task or model was resumed. Rollback needs a compatible binary and trigger/schema manifest; preserve new adjustment/recovery records, never overwrite subsequent user work with an old backup. Windows was not exercised.


## 2026-09-11 actionable chat, retained revisions and recovery

- Root causes verified from local records: a provider timeout ended without final usage; the recovery dialog could amend numeric limits but still rejected that unknown run. Chat recognized the owner's revision confirmation but had no action for confirming the saved related draft or starting a prepared plan. The new planning attempt then failed because read-only research rejected a registered retained worktree's working directory. Once planning succeeded, an idle paused B still held the project's sole execution slot and the coordinator discarded the preparation reason.
- Chat now has exact revision/binding-bound CONFIRM_DRAFT, APPROVE_PLAN and RECOVER_TASK actions, with project/scope checks, durable effects and idempotent handling. Advice/status requests do not authorize execution. A missing plan is still prepared before code execution. Existing scoped small-task advance/pause remain available; project-wide approval is not silently granted from an unrelated small-task chat.
- Unknown completed usage can be explicitly retained and acknowledged while retrying in measurement mode; it is never filled with zero. Old failures, usage, approved QA limits and recovery history remain intact. A new revision starts its own QA history instead of overwriting failed checks. Common recovery actions are visible beside the failure; numeric controls are folded into advanced settings. Technical pre-role failures can be retried without resetting planning or QA. Safe preparation categories now survive the service boundary and explain the next action.
- Original role reports and findings are available through paginated read-only `console_read` reviews, with project/scope isolation. Registered same-repository worktrees are valid read-only research locations; arbitrary outside directories and file changes remain denied. Actual Astra/high planning subsequently completed with 23 tool actions, producing the B revision plan. The original B findings on authenticated URL retention, incomplete model responses and missing real pipeline evidence were explicitly attached to the new task.
- Schema 34 adds PAUSED dispatch state. A paused/ended task with no CREATED/RUNNING model releases its execution slot while its checkout remains owned. The database retains its original unique RESERVED/ACTIVE write-dispatch constraint. Trigger guards reject pausing a live run, resuming without active lifecycle, or reclaiming occupied capacity; provider admission rechecks actual project concurrency. A broader index-predicate change was rejected by automatic review and was not applied. Existing historical receipt fields and all QA outcomes are preserved.
- Read-only UX reference: [Edict](https://github.com/cft0808/edict). Applied task-level state visibility and adjacent intervention controls; the Console keeps project/big-task/subtask context and its own approval semantics. Revision links resolve to the latest planning successor so an old failure does not send the owner back to a superseded plan.
- Activation: private schema-33 backup, idle daemon restart via the original signed desktop entry, schema 33 → 34, all 55 existing table record sets unchanged. Actual B revision resumed on the approved local baseline with one active EXECUTE role. The retained original task remains paused. No Board main commit or remote push, no claim of successful real-provider pipeline acceptance, and no Windows execution.
- Rollback requires a binary that understands PAUSED dispatches and the new recorded action/diagnostic types. Preserve subsequent records and worktrees; never overwrite new work with a pre-upgrade backup. Database backup and activation verification are local/private, not committed.
- Final verification: full suite **204 files / 4,946 tests PASS**, four workers, 778.05 seconds, no failures/errors/skips. Lint, typecheck, build, public hygiene, executable E2E and diff checks PASS. Private-copy Chromium verified the older task's revision link reaches the current successor, active task progress, desktop 1440px and mobile 390px layouts with no horizontal overflow or page errors. Earlier private-copy interaction tests covered draft confirmation and explicit unknown-usage recovery with one simulated start, no production writes or real model turns. Windows was not exercised.


### Retained BLOCKED result re-assessment
Successful provider completion can still return an implementation blocker. The operator can now explicitly re-assess preserved EXECUTE/REPAIR results, through chat recovery or the adjacent “重新核对并继续” button. This creates a fresh authorization on the recorded result candidate; it does not rewrite the old BLOCKED result, reset QA cycles, recover a successful PASS/READY verdict, or skip current scope/candidate/usage/time checks. Missing usage or expired time opens the existing adjustment controls. Implementation READY now explicitly means ready for independent checking, not final acceptance.

Full regression: 204 files / 4,947 tests PASS; required release checks pass. Actual production resume reached independent QA; QA identified only missing authorized live verification after passing 103 local tests and the prior code regressions. User approved a fresh window ending 2026-09-11 13:13:54 Beijing. No API probe ran; integration/fee choice remains pending. Schema 34 and all existing records were unchanged at activation. Windows was not tested.
